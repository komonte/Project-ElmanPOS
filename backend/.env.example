# HTTP Server
PORT=8080

# Copy to .env and adjust. .env is gitignored — never commit real secrets. DB_HOST=localhost
DB_NAME=elman_u
DB_USER=elman_u
DB_PASSWORD=elman_u_dev
DB_PORT=5432
DB_SSLMODE=disable
